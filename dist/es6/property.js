import { pipe } from "fp-ts/lib/function";
// two args, and a compare?
/**
 * @summary
 * Takes a polymorphic  version of Magma's concat,
 * and an EQ for comparison.
 */
export function commutative(f, eq) {
    return pipe();
}
